import type { PartialTheme } from '@pandacss/types'
import { herotextRecipe } from './herotext'

export const presetRecipes: PartialTheme['recipes'] = {
  // overlay: overlayRecipe,
  herotext: herotextRecipe,
}
